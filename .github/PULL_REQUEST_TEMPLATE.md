## What

<!-- One or two sentences: what does this PR change and why? -->

## Checklist

- [ ] `cd engine && uv run ruff check . && uv run pytest` passes
- [ ] `cd app && flutter analyze && flutter test` passes (if app code changed)
- [ ] Docs updated (`README.md` / `docs/`) if behavior or APIs changed
- [ ] No secrets, tokens, or device identifiers committed
